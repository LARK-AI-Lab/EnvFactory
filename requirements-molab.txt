# MoLab runtime/generation environment.
#
# vLLM is intentionally not pinned to the repository's historical 0.8.5
# extra. Install this file with uv's live-driver backend selection:
#   uv pip install --python .venv-mini-runtime/bin/python \
#     --torch-backend=auto -r requirements-molab.txt
vllm
marimo>=0.24,<1
sentence-transformers>=3
psutil>=7,<8
nvidia-ml-py>=12
